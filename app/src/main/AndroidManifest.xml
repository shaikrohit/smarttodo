<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- For re-scheduling alarms on boot -->

    <application
        android:name=".SmartTodoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartToDo"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PomodoroActivity"
            android:theme="@style/Theme.SmartToDo" />

        <activity
            android:name=".ui.TaskDetailActivity"
            android:theme="@style/Theme.SmartToDo" />

        <!-- Task Reminder Receiver -->
        <receiver
            android:name=".receiver.TaskReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Notification Action Receiver -->
        <receiver
            android:name=".receiver.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.example.smarttodo.ACTION_COMPLETE" />
                <action android:name="com.example.smarttodo.ACTION_SNOOZE" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".receiver.SnoozedNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Notification Action Activity for dialog interactions -->
        <activity
            android:name=".ui.NotificationActionActivity"
            android:theme="@style/Theme.App.Translucent"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

    </application>

</manifest>
